{
  "name": "hyperapp-intro-presentation",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "browser-sync start --index index.html -s --port 8000 --files \"./dist/*.js, *.html\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "browser-sync": "^2.23.7"
  }
}
